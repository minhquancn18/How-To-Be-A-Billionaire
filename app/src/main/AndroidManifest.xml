<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myproject22">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyProject22">
        <activity android:name=".View.Activity.ReportCategoryDetailActivity"></activity>
        <!-- Phan report cua Son -->
        <activity android:name=".View.Activity.ReportCategoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.ReportCategoryGraphActivity" /> <!-- phan cua Quan -->
        <activity android:name=".View.Activity.AddingTypeCategoryActivity" />
        <activity android:name=".View.Activity.CategoryActivity" />
        <activity android:name=".View.Activity.AddingActivity" /> <!-- Giao dien chinh -->
        <activity android:name=".View.Activity.DashboardActivity" /> <!-- Phan cua Tam -->
        <activity android:name=".View.Activity.GoalActivity" />
        <activity android:name=".View.Activity.SavingActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>