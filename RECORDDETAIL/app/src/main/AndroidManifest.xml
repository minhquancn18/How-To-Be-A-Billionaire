<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myproject22">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_money"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_money"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyProject22">

        <activity android:name=".View.Activity.RecordDetailActivity"></activity>
        <activity android:name=".View.Activity.ReportCategoryDetailActivity" /> <!-- Phan report cua Son -->
        <activity android:name=".View.Activity.ReportCategoryActivity"></activity>
        <activity android:name=".View.Activity.ReportCategoryGraphActivity"></activity> <!-- phan cua Quan -->
        <activity android:name=".View.Activity.AddingTypeCategoryActivity" />
        <activity android:name=".View.Activity.AddingActivity" /> <!-- Giao dien chinh -->
        <activity android:name=".View.Activity.DashboardActivity" /> <!-- Phan cua Tam -->

        <!-- GOAL PART -->
        <activity android:name=".View.Activity.GoalRecordActivity">


        </activity>
        <activity
            android:name=".View.Activity.NewGoalActivity"
            android:parentActivityName=".View.Activity.GoalActivity" />
        <activity
            android:name=".View.Activity.GoalActivity"
            android:fitsSystemWindows="true"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.myproject22.View.Activity.GoalActivity.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>